<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/vpgImagenes"
        android:layout_width="500dp"
        android:layout_height="500dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"></androidx.viewpager.widget.ViewPager>

    <Button
        android:id="@+id/btnInsertar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Insertar"
        app:layout_constraintTop_toBottomOf="@id/vpgImagenes"
        app:layout_constraintRight_toLeftOf="@id/btnEliminar"
        app:layout_constraintLeft_toLeftOf="parent"></Button>

    <Button
        android:id="@+id/btnEliminar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Eliminar"
        app:layout_constraintTop_toBottomOf="@id/vpgImagenes"
        app:layout_constraintLeft_toRightOf="@id/btnInsertar"
        app:layout_constraintRight_toRightOf="parent"></Button>

    <TextView
        android:id="@+id/txvCantidad"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="20dp"
        android:text="Cantidad de Imágenes"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/btnInsertar"></TextView>

    <EditText
        android:id="@+id/nmpCantidad"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:gravity="center"
        android:inputType="number"
        android:background="@color/white"
        android:layout_margin="50dp"
        app:layout_constraintTop_toBottomOf="@id/txvCantidad"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/btnGuardar"></EditText>

    <Button
        android:id="@+id/btnGuardar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Guardar"
        android:layout_margin="50dp"
        app:layout_constraintTop_toTopOf="@id/nmpCantidad"
        app:layout_constraintBottom_toBottomOf="@id/nmpCantidad"
        app:layout_constraintLeft_toRightOf="@id/nmpCantidad"
        app:layout_constraintRight_toRightOf="parent"></Button>





</androidx.constraintlayout.widget.ConstraintLayout>